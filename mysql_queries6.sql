use real_estate;
select * from employees;
select * from employees order by desc limit 
select employess.emp_id, 
    employees.first_name,
    employees.last_name,
    
from yourtable employees
inner join
(
    select max(emp_id) 
    from yourtable
    group by emp_id;
select * from employees order by emp_id desc limit 1;

select * from login;

insert into login 
values("6","brian","222222","6","1","1","whats your favourite movie?",
"spiderman");

select max(emp_id) from employees ;

select * from login;